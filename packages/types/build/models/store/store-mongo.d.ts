import mongoose from "mongoose";
import { Store } from "./store";
export declare const StoreSchemaMongo: mongoose.Schema<Store, mongoose.Model<Store, any, any, any, any>, {}, {}, {}, {}, mongoose.DefaultSchemaOptions, Store>;
