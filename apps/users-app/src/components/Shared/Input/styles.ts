import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        width: "100%",
        height: 60,
        flexDirection: "row",
        borderWidth: 1,
        alignItems: "center",
        borderRadius: 5,
        backgroundColor: '#FFF',
    },
    rightIcon: {
        position: "absolute",
        right: 15
    }
}) 

export default styles;